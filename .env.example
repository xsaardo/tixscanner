# TixScanner Environment Variables Template
# Copy this file to .env and fill in your actual values
# NEVER commit the .env file to version control!

# Ticketmaster API Configuration
# Get your API key from: https://developer.ticketmaster.com/
TICKETMASTER_API_KEY=your_ticketmaster_api_key_here

# Secure Gmail API Configuration (Recommended)
# Set up OAuth2 credentials from Google Cloud Console
# Download credentials file as 'gmail_credentials.json'
# No passwords needed - uses secure OAuth2 authentication

# Ticketmaster API Configuration remains the same

# Optional: Override recipient email if different from sender
# RECIPIENT_EMAIL=different.email@gmail.com

# Optional: Environment-specific settings
# ENVIRONMENT=development
# DEBUG=true